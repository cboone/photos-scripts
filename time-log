#!/usr/bin/env bash

function main {
  set -x

  local log_name
  if [[ "$2" == 'main' ]]; then
    log_name="$1"
    shift
  else
    log_name=$(basename "$1")
  fi
  local -r log=/Users/hpg/Pictures/Scripts/logs/"${log_name}-$(date-iso8601-ish)"

  "$@" |& ts %H:%M:%.S > "$log" 2>&1
}

main "$@"
